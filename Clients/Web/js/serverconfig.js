// Release configuration
// Do not use the original (deleted that line), use the following one (OguzP)
var baseAddress = 'http://' + window.location.hostname + ':8080'
var catalogBaseAddress = 'http://' + window.location.hostname + ':50001'
var quoteBaseAddress = 'http://' + window.location.hostname + ':50002'
var orderBaseAddress = 'http://' + window.location.hostname + ':8080'
var shipmentBaseAddress = 'http://' + window.location.hostname + ':50004'



